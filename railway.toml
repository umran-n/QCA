[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn src.qca_replication.api:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
